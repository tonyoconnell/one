---
import Layout from "./layout.astro";
const { frontmatter } = Astro.props;
---

<Layout url={frontmatter.url}>
  <h1>{frontmatter.title}</h1>
  <h2>Post author: {frontmatter.author}</h2>
  <slot />
  <a class="btn btn-circle bg-secondary" href="/">home</a>
</Layout>
